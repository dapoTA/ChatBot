## Packages
framer-motion | Smooth animations for chat bubbles and page transitions
date-fns | Formatting timestamps for chat messages
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Using standard REST API for chat (not streaming) based on provided schema.
Using Shadcn UI components that are already present in the project structure.
Font selection: Inter for a clean, enterprise look.
